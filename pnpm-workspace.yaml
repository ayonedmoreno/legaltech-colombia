packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency install scripts by default; allow only what is required.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - esbuild
  - prisma
