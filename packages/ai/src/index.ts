/**
 * Provider-agnostic AI layer. Skeleton only (implemented in the Legal AI phase).
 * Model providers, retrieval and validation will live behind interfaces (PROJECT_SPEC.md s.19).
 */
export const AI_PACKAGE = "@legaltech/ai" as const;
