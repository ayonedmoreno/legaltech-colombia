/**
 * Configurable pricing engine. Skeleton only (implemented in the Pricing phase).
 * Tariffs live in versioned PriceRule data, never in frontend code (PROJECT_SPEC.md s.22).
 * This package must stay free of database, AI and framework imports.
 */
export const PRICING_ENGINE_PACKAGE = "@legaltech/pricing-engine" as const;
