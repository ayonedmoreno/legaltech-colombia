/**
 * Deterministic legal rules engine. Skeleton only (implemented in the Legal AI phase).
 * No legal rule is defined here: every rule must reference a stored legal source
 * (PROJECT_SPEC.md s.20-21). This package must stay free of database, AI and framework imports.
 */
export const LEGAL_ENGINE_PACKAGE = "@legaltech/legal-engine" as const;
